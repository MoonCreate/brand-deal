import { CampaignSubmissionContent } from './content'

export const CampaignSubmission = () => {
  return (
    <main className="w-full h-full flex grow">
      <CampaignSubmissionContent />
    </main>
  )
}
