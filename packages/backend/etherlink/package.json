{
  "name": "etherlink",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ etherlink",
    "create-local": "graph create --node http://localhost:8020/ etherlink",
    "remove-local": "graph remove --node http://localhost:8020/ etherlink",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 etherlink",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.37.0"
  }
}
